# 二级 IP 池（子 IP 池 / 站点级）项目策划书

## 1. 项目概述

二级池是**站点级**的 IP 池：一个站点一个独立进程（单站点独立进程），负责从一级池同步 IP、对目标站点做连通性测试、并按需把可用 IP 租赁给用户。

- 同步：首次全量拉取，之后每 3 秒按 id 增量拉取；增量返回空时按一级池 `max_id` 判定——水位线等于 `max_id` 视为暂无新 IP（不重拉），大于 `max_id`（一级池重启/换代）才全量重拉并重置水位线
- 入池测试：经代理真实访问目标站点，延迟 < 2000ms 才入池（通用站点默认测 `www.baidu.com`）
- 租赁：获取即标记（无过期时间），支持释放/删除/全部释放
- 复验：每 60 秒对池内全部 IP 做「代理可达性」测试（`proxy_reachability_test`，不测出口），删除不通过者
- 拉取与测试解耦：同步拉取与站点测试分离，批次进有界队列，多 worker 并行测试（`test.workers`/`test.buffer` 配置化），队满丢最旧批次（`drops` 累计）

## 2. 项目定位与边界

**只做**：一级池增量同步 → 站点连通测试 → 入池 → 租赁分配/释放/删除 → 周期复验 → 对外 API。
**不做**：供应商拉取（一级池）、按站点路由分发（代理层）。

**对外依赖**：一级池 HTTP API（网络）；`ip_pool_common`（公共库）。
**被依赖**：代理层通过本服务 HTTP API 透传请求（进程级解耦）。

## 3. 技术选型与依赖

| 类别 | 选型 |
|---|---|
| Web 框架 | FastAPI + uvicorn |
| 并发模型 | asyncio（单事件循环，锁 + 信号量） |
| HTTP 客户端 | aiohttp（含 aiohttp-socks）；可达性握手用 python-socks |
| 配置 | pydantic-settings + PyYAML |
| 公共库 | ip_pool_common |
| 测试 | pytest + pytest-asyncio + aioresponses |

## 4. 系统架构

```
一级池 HTTP API (:8000)
        │ HTTP (全量 / 按id增量)
        ▼
┌────────────────────────────────────────┐
│              二级 IP 池 (:8001~)        │
│  ┌──────────────────────────────────┐  │
│  │ Syncer(增量同步 + max_id 判定)    │  │  维护 last_synced_id 水位线
│  │  → 有界队列(buffer) 多worker      │  │  拉取与测试解耦，队满丢最旧(drops)
│  └──────────────┬───────────────────┘  │
│                 ▼ 多 worker 并发        │
│  ┌──────────────────────────────────┐  │
│  │ Tester(站点连通测试 <2000ms)      │  │  site_test 真实出口测试 + 批次汇总日志
│  └──────────────┬───────────────────┘  │
│                 ▼                       │
│  ┌──────────────────────────────────┐  │
│  │ Level2Pool(租赁池)               │  │  键=proxy_url；本地自增id；租赁标记
│  └──────────────┬───────────────────┘  │
│                 │                       │
│  ┌──────────────▼───────────────────┐  │
│  │ Routes(HTTP API)                 │  │  status/count/ips/acquire/release/delete/release-all
│  └──────────────────────────────────┘  │
│  后台任务: SyncTask(拉取+worker) / RevalidateTask / TtlSweeper │
└────────────────────────────────────────┘
        ▲ HTTP
        │
     代理层(透传)
```

## 5. 文件结构

```
level2_pool/
├── 项目策划书.md
├── requirements.txt
├── config/
│   ├── level2_pool.example.yaml      # 每站点一份配置模板（入库）
│   └── level2_pool.yaml              # 运行期真实配置（gitignore，由模板复制）
├── app/
│   ├── __init__.py
│   ├── main.py                       # FastAPI 装配 + lifespan 启停任务
│   ├── config.py                     # Level2Settings
│   ├── syncer.py                     # Level1SyncClient + SyncTask(增量/max_id 判定 + 多 worker 管线)
│   ├── pool.py                       # Level2Pool(租赁池)
│   ├── tester.py                     # 站点测试 + 周期复验封装（含批次汇总日志）
│   ├── tasks.py                      # SyncTask(再导出)/RevalidateTask/TtlSweeper
│   └── routes.py                     # 7 个 API 路由
└── tests/
    ├── test_pool.py                  # 租赁/释放/删除/最新优先分配/计数细分
    ├── test_syncer.py                # 增量、max_id 判定、水位线、多 worker 管线
    ├── test_tester.py                # 站点测试阈值/复验/汇总日志
    ├── test_tasks.py                 # 复验/TTL 后台任务
    ├── test_routes.py                # API 契约与统计字段
    ├── test_integration.py           # mock 一级池闭环
    ├── test_perf.py                  # 压测
    └── test_smoke.py                 # 配置加载冒烟
```

## 6. 数据模型

复用公共库：`Protocol`、`Level2Record`、`build_proxy_url`、`site_test`、`proxy_reachability_test`。

```python
@dataclass
class PoolStats:                     # count/status 的统计数据
    total: int                       # 池内总数
    by_proto: dict[Protocol, int]    # 池内各协议数
    leased_total: int                # 已租赁总数
    leased_by_proto: dict[Protocol, int]
    free_total: int                  # 空闲总数
    free_by_proto: dict[Protocol, int]

@dataclass
class ServiceStats:
    uptime: float
    total_pulled: int                # 从一级池拉取总数
    total_entered: int               # 入池(站点测试通过)总数
    api_call_count: int
    last_synced_id: int | None       # 当前水位线(一级池id)
    sync_failures: int               # 同步失败次数
    test_failures: int               # 站点测试批次异常次数
    revalidate_failures: int         # 复验异常次数
    ttl_sweep_failures: int          # TTL 清扫异常次数
    drops: int                       # 队满丢弃批次累计数
    empty_acquires: int              # acquire 空池/全租赁次数
```

## 7. 核心模块设计

### 7.1 syncer.py —— 增量同步（max_id 判定 + 拉取/测试解耦）

```python
class Level1SyncClient:
    def __init__(self, base_url: str, session: aiohttp.ClientSession): ...
    async def fetch_all(self) -> list[IpRecord]              # GET /api/v1/ips
    async def fetch_after(self, id_: int) -> tuple[list[IpRecord], int | None]
        # GET /api/v1/ips/after/{id} → (记录, 一级池顶层 max_id)

class SyncTask:
    last_synced_id: int | None = None
    async def _sync_once(self):          # 拉取阶段：只拉取、推进水位线、入队，不做测试
        if self.last_synced_id is None:
            batch = await self.client.fetch_all()            # 首次全量
        else:
            batch, max_id = await self.client.fetch_after(self.last_synced_id)
            if not batch and (max_id is None or self.last_synced_id > max_id):
                # 空响应且水位线越过一级池最大 id → 重启/换代 → 重新全量拉取
                batch = await self.client.fetch_all()
            # 空响应且水位线 == max_id → 一级池暂无新 IP，跳过（不重拉）
        if batch:
            self.last_synced_id = max(r.id for r in batch)   # 水位线推进/重置
            self.stats.total_pulled += len(batch)
            self._enqueue(batch)                             # 入有界队列
    async def _run_worker(self):         # 测试 worker 并发消费
        passed = await self.tester.site_filter(batch)        # 站点测试 <2000ms
        for rec in passed: await self.pool.upsert(rec)
    async def run(self):                  # 每 sync_interval(3s) 循环；启动多 worker（默认 max(1, concurrency//10)）
```

**关键点（空响应 max_id 判定）**：
- 增量返回空时依据一级池顶层 `max_id` 区分：
  - `last_synced_id == max_id`：一级池暂无新 IP，**跳过**（不做全量提取）；
  - `last_synced_id > max_id`：一级池 id 空间已重置（重启/换代）→ 全量重拉并重置水位线；
  - `max_id` 缺失（旧版/空池）→ 回退全量重拉，保持向后兼容。
- 重置后按代理身份（`proxy_url`）去重 upsert，**不移除**现存记录（空闲与租赁均保留），旧记录靠 60s 复验与 TTL 自然淘汰。
- 一级池 id 仅作水位线，由 syncer 内部维护，**不参与二级池记录身份**，彻底规避一级池重启后 id 空间重叠的冲突。
- 拉取与测试解耦：拉取按 tick 起点计时，慢测试不阻塞拉取节奏；批次进有界队列（`test.buffer`），队满丢最旧批次（`drops` 累计），被丢弃的 IP 仍计入 `total_pulled`。

### 7.2 pool.py —— 租赁池

```python
class Level2Pool:
    def __init__(self):
        self._records: dict[str, Level2Record] = {}    # 唯一键 proxy_url(ip+port+protocol)
        self._order: list[str] = []                     # 入池顺序，最新在末尾
        self._id_index: dict[int, str] = {}             # 本地id → proxy_url
        self._next_id: int = 0
        self._lock = asyncio.Lock()

    async def upsert(self, rec: Level2Record):          # 存在则刷新(延迟/ttl/时间)，新则分配本地id
    async def acquire(self) -> Level2Record | None:     # 最新→最旧遍历，取第一个空闲并标记租赁
    async def release(self, id_: int) -> bool           # 解除租赁标记
    async def remove(self, id_: int) -> bool            # 删除记录(含解除租赁)
    async def release_all(self) -> int                  # 解除全部租赁，返回数量
    def all(self) -> list[Level2Record]                 # 查询，不产生租赁标记
    def stats(self) -> PoolStats                        # 池内/租赁/空闲 按协议细分
    async def sweep_ttl(self, now: float) -> int        # TTL 淘汰(若支持)
```

**关键点**：
- 唯一键为 `proxy_url`（代理身份），本地 id 自增且不复用，供 API `release/delete/{id}` 精确引用。
- `acquire` 最新优先：从 `_order` 尾部向前扫描首个 `leased=False`，原子标记，百并发下经 `asyncio.Lock` 保证不重复分配。
- 租赁无过期时间，仅可被显式 `release/delete/release-all` 解除。

### 7.3 tester.py —— 站点测试与复验

```python
class Tester:
    def __init__(self, target_url, threshold_ms=2000, timeout=3.0, concurrency=20): ...
    async def site_filter(self, records) -> list[Level2Record]:
        # site_test(proxy, target) 真实出口测试；ok 且 latency < 2000ms 才通过
    async def revalidate(self, records) -> list[Level2Record]:
        # proxy_reachability_test 仅代理可达性(不测出口)；返回仍存活的项
    @staticmethod
    def _log_batch(label, total, ok_count, reasons): ...
        # 每批测试后输出汇总日志：total=X ok=Y fail=Z (timeout*10, connect*3, ...)
        # reasons 为失败原因计数（timeout/connect/proxy_reject/http_5xx/slow/...）
```

### 7.4 tasks.py —— 后台任务

```python
from .syncer import SyncTask          # 实际定义在 syncer.py，此处再导出
class RevalidateTask:  # 每 60s：全量代理可达性复验 → 删除不通过项（含租赁中的）
class TtlSweeper:      # 每 5s：TTL 淘汰
```

## 8. 核心流程

```
启动 → 创建 sync client/pool/tester → 启动 SyncTask/RevalidateTask/TtlSweeper
SyncTask 每3s: 增量(after/{id}) → 空时按 max_id 判定（==不重拉、>才全量重置、缺失回退） → 批次入队 → 多 worker 站点测试<2000ms → upsert 入池
RevalidateTask 每60s: 全量 proxy_reachability_test → 失败者删除
TtlSweeper 每5s: 清扫 TTL 过期(若支持)
用户 acquire: 最新优先取空闲 → 标记租赁 → 返回 proxy_url
用户 release/delete/release-all: 解除/删除标记
关闭: 取消任务 → 关闭 session → 优雅退出
```

## 9. API 设计

基路径 `/api/v1`。

| 方法 | 路径 | 说明 | 返回 data 结构 |
|---|---|---|---|
| GET | `/status` | 运行状态 | `uptime,total_pulled,total_entered,api_call_count,last_synced_id,errors{sync_failures,test_failures,revalidate_failures,ttl_sweep_failures,empty_acquires},drops,pool_stats` |
| GET | `/count` | 计数 | `total,by_proto,leased_total,leased_by_proto,free_total,free_by_proto` |
| GET | `/ips` | 全部 IP（不标记） | `[{id,ip,port,protocol,proxy_url,latency_ms,leased,ttl,created_at}]` |
| POST | `/ips/acquire` | 获取 1 个空闲 IP 并租赁 | 单条记录或 `{code:40402}` 空池 |
| POST | `/ips/{id}/release` | 释放指定 IP | 释放结果 |
| DELETE | `/ips/{id}` | 删除指定 IP | 删除结果 |
| POST | `/ips/release-all` | 释放全部 | 释放数量 |

## 10. 配置设计（config/level2_pool.example.yaml）

```yaml
service:
  host: 0.0.0.0
  port: 8001                  # 每站点独立端口
  log_level: INFO

site:
  name: site_a                # 站点标识(代理层路由键)
  target_url: https://www.example.com   # 通用站点填 http://www.baidu.com

level1:
  base_url: http://127.0.0.1:8000

sync:
  interval: 3.0               # 增量同步周期
  timeout: 5.0

test:
  latency_threshold_ms: 2000  # 入池延迟阈值
  connect_timeout: 3.0
  concurrency: 20             # 站点测试并发
  workers: 2                  # 测试 worker 数（缺省自动 max(1, concurrency//10)）
  buffer: 20                  # 待测批次有界队列容量（队满丢最旧，drops 累计）

revalidate_interval: 60.0     # 复验周期(代理可达性)
ttl_sweep_interval: 5.0
```

> 运行期真实配置 `config/level2_pool.yaml` 已 gitignore，由模板 `config/level2_pool.example.yaml` 复制并修改 `site.name`/`target_url`/`service.port`。

## 11. 鲁棒性与容错

- **同步异常隔离**：一级池故障/网络抖动不影响现有租赁；下次 tick 自动重试。
- **空响应 max_id 判定**：一级池重启可无感恢复（全量重拉 + 水位线重置），且不误删现存记录；水位线等于 `max_id` 时不做无谓的全量提取。
- **拉取/测试解耦**：慢测试不阻塞同步节奏；待测批次有界队列（`test.buffer`），队满丢最旧（`drops` 累计），无内存膨胀。
- **并发安全**：池变更、租赁分配全部在 `asyncio.Lock` 下；`acquire` 原子，不重复分配同一 IP。
- **超时控制**：站点测试/复验/同步均设 aiohttp 超时。
- **空池处理**：无空闲 IP 时 `acquire` 返回明确错误码（非 500），调用方可重试。
- **优雅启停**：`lifespan` 统一管理任务生命周期。

## 12. 可扩展性

- 站点测试逻辑可注入（替换为其它站点验证策略）。
- 复验策略可替换（如仅 TTL 淘汰、或质量评分）。
- 阈值/周期全部配置化。
- 单站点独立进程：水平扩展只需为新站点复制一份配置并换端口，互不影响。

## 13. 测试策略

- `test_pool.py`：upsert 去重、最新优先分配、租赁/释放/删除/全部释放、计数细分、本地 id 不复用。
- `test_syncer.py`：首次全量、增量推进水位线、空响应 `max_id` 判定（== 不重拉、> 全量重置、缺失回退）、重置不移除现存记录、worker 管线（并行/队满丢批/慢测试不阻塞拉取）。
- `test_tester.py`：延迟阈值、超时、复验代理可达性、批次汇总日志（total/ok/fail + 失败原因计数）。
- `test_routes.py`：aioresponses 模拟一级池，验证各 API 契约、统计字段、空池错误码、api_call_count 递增。

## 14. 实施步骤

1. 搭骨架：requirements、config、main 装配、公共库接入。
2. 实现 `Level2Pool` + 单测（租赁语义是核心）。
3. 实现 `Level1SyncClient/SyncTask` + 单测（含空响应 max_id 判定）。
4. 实现 `Tester/RevalidateTask/TtlSweeper`。
5. 实现 routes + 中间件统计 + 单测。
6. 与一级池 + mock 供应商端到端联调，验证同步/租赁/复验闭环。
