# 一级 IP 池（主 IP 池）测试计划书

## 1. 测试目标与范围

验证一级池**拉取、代理可达性测试、环形池管理、TTL 淘汰、统计与对外 API** 的正确性、并发安全性与鲁棒性。

**范围**：`app/` 下 `provider.py`、`pool.py`、`tester.py`、`tasks.py`、`routes.py`。
**不在范围**：供应商真实服务（用 `tests/mock_provider.py` 桩替代）、二级池/代理层。

## 2. 测试环境与工具

| 项 | 说明 |
|---|---|
| Python | 3.14 |
| 测试框架 | pytest + pytest-asyncio |
| 网络桩 | aioresponses + 本地 `mock_provider.py`（FastAPI 模拟供应商） |
| 覆盖率 | pytest-cov |
| 依赖文件 | `requirements-dev.txt`：`pytest, pytest-asyncio, pytest-cov, aioresponses` |

## 3. 测试层级

| 层级 | 说明 |
|---|---|
| 单元测试 | pool/provider/tester/tasks 纯逻辑，网络打桩 |
| 集成测试 | mock 供应商 → 拉取 → 测试 → 入池 → API 闭环 |
| 端到端 | 与二级池联调（跨项目，见二级池测试计划书） |
| 性能测试 | 拉取吞吐、池查询延迟、内存稳定性 |
| 鲁棒性测试 | 供应商故障、超时、高并发竞态 |

## 4. 测试用例

### 4.1 pool.py（核心）

| ID | 用例 | 前置 | 步骤 | 预期 | 优先级 |
|---|---|---|---|---|---|
| L1-POOL-001 | `add` 分配自增 id | 空池 | 依次 add | 首个 id=0，逐个 +1，绝不复用 | P0 |
| L1-POOL-002 | 环形淘汰（容量上限） | max=500 或缩小验证 | 入池超过上限 | 最旧记录被弹出，池恒 ≤ 上限 | P0 |
| L1-POOL-003 | 协议计数增量维护 | 混合协议入池 | 查询 `counts()` | http/https/socks4/socks5 数精确 | P0 |
| L1-POOL-004 | 淘汰时计数同步减 | 满池再入 | 弹最旧 | 对应协议计数同步减 | P0 |
| L1-POOL-005 | TTL 清扫 | 含过期与未过期项 | `sweep_ttl(now)` | 仅删 `created_at+ttl<now`，返回删除数 | P0 |
| L1-POOL-006 | 不支持 TTL 不清扫 | 记录 ttl=None | `sweep_ttl` | 删除 0 | P0 |
| L1-POOL-007 | `after(id)` 边界 | 多记录 | id 越界 / 中间 id | 越界返回空数组；中间返回其后全部 | P0 |
| L1-POOL-008 | `all()` 顺序 | 多记录 | 查询 | 按入池顺序返回 | P1 |
| L1-POOL-009 | 并发 `add` | 多协程同时 add | 汇总 | 无丢失、id 唯一、计数一致 | P0 |
| L1-POOL-010 | `size()`/`counts()` 一致性 | 随机操作后 | 对比 | `size == counts.total` | P1 |

### 4.2 provider.py

| ID | 用例 | 前置 | 步骤 | 预期 | 优先级 |
|---|---|---|---|---|---|
| L1-PROV-001 | 工厂按 type 选择子类 | 注册两个子类 | `create("default_http")` | 返回对应实例 | P0 |
| L1-PROV-002 | 未知 type | 未注册类型 | `create("xxx")` | 抛明确异常 | P0 |
| L1-PROV-003 | 子类解析供应商响应 | 桩：标准响应 | `pull(count)` | 输出规范化 `ProviderIp`（含 region/ttl 可选） | P0 |
| L1-PROV-004 | 供应商返回空/异常结构 | 桩：空/畸形 | `pull` | 返回空，不崩溃，记日志 | P1 |
| L1-PROV-005 | 供应商返回数量 ≤ count | 桩：超额 | `pull(count)` | 返回数量受 count 约束 | P1 |
| L1-PROV-006 | `close()` 释放资源 | 已建 session | `close()` | 无异常，可重复调用 | P2 |

### 4.3 tester.py

| ID | 用例 | 前置 | 步骤 | 预期 | 优先级 |
|---|---|---|---|---|---|
| L1-TEST-001 | 仅返回通过项 | 混合通过/失败 | `test_many` | 仅通过者返回，`last_verified_at` 更新 | P0 |
| L1-TEST-002 | 不测出口 | 桩：代理可达但出口不可达 | `test_many` | 仍判通过（只验代理连接） | P0 |
| L1-TEST-003 | 并发受控 | 大量输入 | `test_many` | 并发 ≤ concurrency | P1 |

### 4.4 tasks.py

| ID | 用例 | 前置 | 步骤 | 预期 | 优先级 |
|---|---|---|---|---|---|
| L1-TASK-001 | 拉取限频 ≥1s | 假时钟 | 连续两 tick | 间隔 ≥ `pull_interval` | P0 |
| L1-TASK-002 | 拉取异常不中断循环 | 供应商抛异常 | 运行多 tick | 循环继续，记 WARN | P0 |
| L1-TASK-003 | 统计正确累加 | 部分通过 | 一 tick | `total_pulled`=拉取数，`total_entered`=通过数 | P0 |
| L1-TASK-004 | TTL 清扫周期执行 | 含过期项 | 运行清扫 tick | 过期项被删 | P1 |

### 4.5 routes.py（API）

| ID | 用例 | 前置 | 步骤 | 预期 | 优先级 |
|---|---|---|---|---|---|
| L1-RT-001 | `/status` 字段完整 | 运行态 | GET | 含 uptime/total_pulled/total_entered/pool_size/counts/api_call_count/next_id/errors/drops | P0 |
| L1-RT-002 | `/count` 计数 | 混合协议 | GET | pool_size + 四协议数正确 | P0 |
| L1-RT-003 | `/ips` 全部 | 多记录 | GET | 返回全部含 id/protocol/proxy_url/ttl/created_at | P0 |
| L1-RT-004 | `/ips/after/{id}` | 多记录 | GET 中间 id | 仅返回 id 之后 | P0 |
| L1-RT-004b | `/ips/after/{id}` 携带 max_id | 多记录 | GET 中间 id | 响应顶层 `max_id` == 池内最大 id | P0 |
| L1-RT-005 | `/ips/after/{id}` 越界 | 多记录 | GET 大 id | 返回空数组、`max_id` 正常 | P1 |
| L1-RT-006 | api_call_count 递增 | 连续请求 | 多次 GET | 计数逐次 +1 | P0 |
| L1-RT-007 | 空池查询 | 空池 | GET `/ips` `/count` | 返回空数组 / 0，非 500 | P1 |

## 5. 集成测试

| ID | 场景 | 预期 | 优先级 |
|---|---|---|---|
| L1-INT-001 | mock 供应商 → 拉取 → 测试 → 入池 → API | 闭环，`/ips` 返回入池 IP | P0 |
| L1-INT-002 | 容量上限 + 持续拉取 | 池 ≤500，最旧被替换 | P0 |
| L1-INT-003 | 供应商挂掉 → 恢复 | 期间不崩，恢复后自动续拉 | P0 |

## 6. 鲁棒性与性能测试

| ID | 场景 | 预期 | 优先级 |
|---|---|---|---|
| L1-ROB-001 | 供应商慢/超时 | 超时受控，不悬挂 | P0 |
| L1-ROB-002 | 高并发 add/all | 锁保护无竞态、计数一致 | P0 |
| L1-PERF-001 | 持续拉取吞吐 | ≥10/s 入池，内存不增长 | P1 |
| L1-PERF-002 | 500 池查询延迟 | `/ips`、`/count` <10ms | P1 |

## 7. 测试桩与 Fixture 设计

- `tests/mock_provider.py`：本地 FastAPI 服务，模拟供应商接口，可配置返回数量/协议/失败率/延迟，供集成与性能测试。
- `conftest.py`：
  - `event_loop`、`mock_session`（aioresponses）
  - `mock_provider_cfg`：指向 mock 供应商的配置
  - `pool`：预置多协议记录的 Level1Pool fixture
- 桩对象：供应商正常响应 / 空响应 / 畸形响应 / 超时 / 500。

## 8. 覆盖率目标

| 模块 | 目标 |
|---|---|
| pool / provider / routes | ≥ 95% |
| tester / tasks | ≥ 90% |
| 整体 | ≥ 90% |

## 9. 执行方式

```bash
pip install -r requirements-dev.txt

# 单元 + 集成
pytest -v --cov=app --cov-report=term-missing

# 仅 pool
pytest tests/test_pool.py -v

# 集成（需先起 mock_provider）
pytest tests/test_integration.py -v

# 性能
pytest tests/test_perf.py -v -m perf
```

## 10. 缺陷管理与退出准则

- **缺陷分级**：P0（阻断）必须修复复测；P1（重要）需修复；P2 可排期。
- **退出准则**：P0/P1 清零；pool/provider/routes 覆盖率 ≥ 95%；全部用例通过后方可发布。
- 一级池改动需回归二级池同步相关用例（跨项目契约）。
