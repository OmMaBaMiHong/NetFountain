# 一级 IP 池（主 IP 池）项目策划书

## 1. 项目概述

一级池（主 IP 池）是整个代理 IP 体系的上游数据源。它**只负责从公网 IP 池（供应商）拉取 IP 并做「代理侧」连通性测试**，测试通过者放入内存环形池对外提供。它不关心具体站点，不关心使用者是谁。

- 拉取节奏：每 1 秒拉取 10 个（受供应商限频，严格限频）
- 入池测试：仅验证「能否连接代理」（`proxy_reachability_test`），**不测出口**
- 池容量：上限 500，循环队列淘汰最旧；供应商支持 TTL 时按时间淘汰
- 对外：HTTP API（供二级池增量同步）

## 2. 项目定位与边界

**只做**：拉取 → 代理可达性测试 → 入池 → 淘汰 → 对外查询 API。
**不做**：站点级测试、租赁管理、用户侧代理——这些分属二级池与代理层。

**对外依赖**：供应商公网 IP 池 API（网络）；`ip_pool_common`（公共库）。
**被依赖**：二级池通过本服务 HTTP API 增量同步（进程级解耦，非代码依赖）。

## 3. 技术选型与依赖

| 类别 | 选型 |
|---|---|
| Web 框架 | FastAPI + uvicorn |
| 并发模型 | asyncio（单事件循环，锁 + 信号量） |
| HTTP 客户端 | aiohttp（含 aiohttp-socks）；可达性握手用 python-socks |
| 配置 | pydantic-settings + PyYAML |
| 公共库 | ip_pool_common |
| 测试 | pytest + pytest-asyncio + aioresponses |

## 4. 系统架构

```
供应商公网IP池 (1s/10个)
        │ HTTP
        ▼
┌─────────────────────────────────┐
│           一级 IP 池             │
│  ┌───────────────────────────┐  │
│  │ ProviderClient(派生)      │  │   BaseProvider 抽象 + 子类 + 工厂
│  │  default_http / http91    │  │   http91 携带 expire_time 折算 TTL
│  └───────────┬───────────────┘  │
│              ▼                   │
│  ┌───────────────────────────┐  │
│  │ 拉取循环 PullTask          │  │   拉取与测试解耦，按 tick 计时保 1s 节奏
│  │  → 有界队列(buffer)        │  │   队满丢最旧批次(drops)
│  └───────────┬───────────────┘  │
│              ▼ 多 worker 并发    │
│  ┌───────────────────────────┐  │
│  │ Tester(代理可达性测试)     │  │   proxy_reachability_test 纯 python_socks 握手
│  └───────────┬───────────────┘  │
│              ▼                   │
│  ┌───────────────────────────┐  │
│  │ Level1Pool(环形池500+TTL) │  │   deque(maxlen=500) + dict索引 + 协议计数
│  │  proxy_url 去重(删除重建)  │  │   重复入池刷新 ttl/region，新 id 触发增量同步
│  └───────────┬───────────────┘  │
│              │                   │
│  ┌───────────▼───────────────┐  │
│  │ Routes(HTTP API)          │  │   status/count/ips/ips-after-id
│  └───────────────────────────┘  │
│  后台任务: PullTask(拉取+worker) / TtlSweeper │
└─────────────────────────────────┘
        ▲ HTTP
        │
     二级池(增量同步)
```

## 5. 文件结构

```
level1_pool/
├── 项目策划书.md
├── requirements.txt
├── config/
│   ├── level1_pool.example.yaml        # 配置模板（含 91HTTP 凭据占位，入库）
│   └── level1_pool.yaml                # 运行期真实配置（gitignore，由模板复制）
├── app/
│   ├── __init__.py
│   ├── main.py                       # FastAPI 装配 + lifespan 启停后台任务
│   ├── config.py                     # Level1Settings(pydantic-settings)
│   ├── provider.py                   # BaseProvider + DefaultHttpProvider + Http91Provider + ProviderFactory
│   ├── pool.py                       # Level1Pool 环形池(proxy_url 去重)
│   ├── tester.py                     # 批量代理可达性测试封装
│   ├── tasks.py                      # PullTask(拉取+多worker测试管线) / TtlSweeper
│   └── routes.py                     # 4 个 API 路由
└── tests/
    ├── mock_provider.py              # 本地模拟供应商(供单测/联调)
    ├── test_pool.py                  # 环形淘汰/TTL/计数/after-id/proxy_url 去重
    ├── test_provider.py              # 工厂选择、子类解析(含 http91)、限频
    ├── test_tester.py                # 可达性测试封装
    ├── test_tasks.py                 # 拉取/worker 管线/TTL 清扫
    ├── test_routes.py                # API 契约与统计字段
    ├── test_integration.py           # mock 供应商闭环
    ├── test_perf.py                  # 吞吐与延迟
    └── test_smoke.py                 # 配置加载冒烟
```

## 6. 数据模型

复用公共库：`Protocol`、`ProviderIp`、`IpRecord`、`build_proxy_url`。

一级池内部计数结构：

```python
@dataclass
class PoolCounts:
    total: int
    http: int
    https: int
    socks4: int
    socks5: int

@dataclass
class ServiceStats:          # 状态/统计（随请求实时快照）
    uptime: float             # 运行秒数
    total_pulled: int         # 已拉取(供应商返回)总数
    total_entered: int        # 入池(测试通过)总数
    api_call_count: int       # API 调用次数(中间件累计)
    next_id: int              # 下一个将分配的 id
    pull_failures: int        # 供应商拉取失败次数
    test_failures: int        # 测试批次异常次数
    ttl_sweep_failures: int   # TTL 清扫异常次数
    drops: int                # 队满丢弃批次累计数
```

## 7. 核心模块设计

### 7.1 provider.py —— 供应商客户端（继承派生，高扩展性）

```python
class BaseProvider(ABC):
    name: ClassVar[str]
    def __init__(self, cfg: dict, session: aiohttp.ClientSession): ...
    @abstractmethod
    async def pull(self, count: int) -> list[ProviderIp]: ...
    async def close(self): ...

# 示例：默认 HTTP 供应商
@register("default_http")
class DefaultHttpProvider(BaseProvider):
    async def pull(self, count): 
        # GET api_url(count, api_key) → 解析 {data:[{ip,port,protocol,region,ttl}]}
        ...

# 91HTTP 供应商（真实商用）：
@register("http91")
class Http91Provider(BaseProvider):
    async def pull(self, count):
        # GET api_url(trade_no, secret, num, format=json, time=1, protocol)
        # 解析 {code,data:{proxy_list:[{ip,port,expire_time}]}}
        # expire_time 绝对时间 → 剩余秒数写入 ProviderIp.ttl
        # protocol: 1=HTTP, 2=SOCKS5

# 扩展：新增供应商 = 新增一个子类并注册
@register("supplier_x")
class SupplierXProvider(BaseProvider):
    ...

class ProviderFactory:
    _registry: dict[str, type[BaseProvider]] = {}
    @classmethod
    def register(cls, name, provider_cls): ...
    @classmethod
    def create(cls, provider_type: str, cfg: dict, session) -> BaseProvider:
        # 按 config.provider.type 实例化；未知类型抛明确异常
```

**扩展性**：新增供应商只需「新增子类 + `@register("名字")`」，主流程（拉取/测试/入池）零改动。

### 7.2 pool.py —— 环形池（循环队列 + TTL + 协议计数）

```python
class Level1Pool:
    def __init__(self, max_size: int = 500):
        self._records: dict[int, IpRecord] = {}       # id → 记录
        self._order: deque[int] = deque(maxlen=max_size)  # 循环队列，满则弹出最旧
        self._counts: dict[Protocol, int] = Counter()
        self._key_index: dict[str, int] = {}          # proxy_url → id（去重索引）
        self._duplicates: int = 0                     # 因 proxy_url 重复被删除重建的累计次数
        self._next_id: int = 0
        self._lock = asyncio.Lock()

    async def add(self, ip: ProviderIp, now: float) -> IpRecord:
        # 按 proxy_url(ip+port+protocol) 去重：重复则删除旧记录并分配新 id 重建
        #   （刷新 ttl/region，移至队尾，新 id 触发二级池增量同步，duplicates 累计）
        # 新记录分配自增id → 入 dict → 入 deque(maxlen 满自动丢弃最旧并同步计数) → 计数+1
    async def sweep_ttl(self, now: float) -> int:
        # 遍历按 created_at+ttl 删除过期项，返回删除数（供应商支持TTL时生效）
    def size(self) -> int
    def counts(self) -> PoolCounts
    @property
    def max_id(self) -> int | None                 # 当前池内最大 id（池空 None），O(1)
    @property
    def duplicates(self) -> int                    # proxy_url 重复被刷新重建的累计次数
    async def all(self) -> list[IpRecord]                       # 按入池顺序
    async def after(self, id_: int) -> list[IpRecord]           # id > id_ 的全部
```

**关键点**：
- 容量淘汰由 `deque(maxlen=500)` 天然实现，O(1)；TTL 淘汰为周期性清扫，二者独立，TTL 优先于容量兜底。
- 以 `proxy_url`（ip+port+protocol）为去重键：重复入池时**删除旧记录、分配新 id 重建**（刷新 ttl/region、记录移至尾部）。新 id 作为增量同步水位线的推进依据，保证二级池能感知刷新；`duplicates` 累计刷新次数，随 `/status` 暴露（`total_duplicates`）。
- 所有变更在 `asyncio.Lock` 下进行，保证并发安全。
- `id` 全局自增、绝不复用，作为二级池增量同步的水位线依据。

### 7.3 tester.py —— 代理可达性测试

```python
class Tester:
    def __init__(self, timeout: float, concurrency: int): ...
    async def test_many(self, ips: list[ProviderIp]) -> list[ProviderIp]:
        # 用 ip_pool_common.batch_test + proxy_reachability_test 并发测试
        # 仅返回通过项；记录 last_verified_at
```

### 7.4 tasks.py —— 后台任务（拉取与测试解耦 + 多 worker 测试管线）

```python
class PullTask:
    async def run(self):
        # 启动 test_workers 个测试 worker（默认 max(1, test_concurrency//pull_count)）
        while not cancelled:
            start = time.monotonic()
            async with self._pull_lock:                    # 只保护拉取（供应商限频点）
                raw = await self.provider.pull(cfg.pull_count)
                self.stats.total_pulled += len(raw)
                if raw: self._enqueue(raw)                 # 入有界队列(buffer)，不等测试
            await asyncio.sleep(max(0, pull_interval - (now-start)))  # 按 tick 起点计时保 1s 节奏
    async def _run_worker(self):                           # 测试 worker 并发消费
        while True:
            raw = await self._queue.get()
            passed = await self._tester.test_many(raw)     # 每批内部按 test_concurrency 并发
            for ip in passed: await self._pool.add(ip, time())
            self.stats.total_entered += len(passed)
    def _enqueue(self, raw):
        # 队满时丢最旧待测批次并累计 drops/stats.drops（有界内存），
        # 被丢弃的 IP 仍计入 total_pulled

class TtlSweeper:
    async def run(self):                                    # 每 cfg.ttl_sweep_interval(默认5s)
        removed = await self.pool.sweep_ttl(time())
```

**鲁棒性**：拉取/测试任何异常仅记录日志，不中断循环；下次 tick 自动重试。拉取与测试解耦后，测试耗时不拖慢 1s 拉取节奏；队列有界，供应商突发快拉时不会撑爆内存（丢最旧批次兜底）。

### 7.5 routes.py —— API

见第 9 节。统一响应 `{code, msg, data}`，API 调用计数由 `ApiCounterMiddleware` 累计。

## 8. 核心流程

```
启动(lifespan) → 创建 ProviderFactory 实例 + Level1Pool + 启动 PullTask/TtlSweeper
PullTask: 每1s 拉取(锁内) → 批次入队 → 多 worker 并发代理可达性测试(纯握手) → 通过者入池(proxy_url 去重) → 统计累加
TtlSweeper: 每5s 清扫 TTL 过期项(若供应商支持TTL)
API 查询: 直接读内存池快照(加锁)；/status 含 total_duplicates
关闭: 取消后台任务 → 关闭 provider/session → 优雅退出
```

## 9. API 设计

基路径 `/api/v1`。

| 方法 | 路径 | 说明 | 返回 data 结构 |
|---|---|---|---|
| GET | `/status` | 运行状态 | `uptime,total_pulled,total_entered,total_duplicates,pool_size,counts{http,https,socks4,socks5},api_call_count,next_id,errors{pull_failures,test_failures,ttl_sweep_failures},drops` |
| GET | `/count` | 池内计数 | `pool_size, counts{http,https,socks4,socks5}` |
| GET | `/ips` | 全部 IP | `[{id,ip,port,protocol,proxy_url,region,ttl,created_at}]` |
| GET | `/ips/after/{id}` | 指定 id 之后的 IP | 同上；响应顶层含 `max_id`（当前池内最大 id，池空为 null），供二级池判定是否全量重拉 |

## 10. 配置设计（config/level1_pool.example.yaml）

```yaml
service:
  host: 0.0.0.0
  port: 8000
  log_level: INFO

provider:
  type: http91                # 供应商类型：http91 | default_http
  api_url: http://api.91http.com/v1/get-ip
  api_key: "<你的密钥>"        # 91HTTP 为 secret（运行期配置，gitignore）
  trade_no: "<你的业务编号>"   # 91HTTP 专用
  protocol: 1                 # 91HTTP：1=HTTP，2=SOCKS5
  pull_count: 10              # 每次拉取数量
  pull_interval: 1.0          # 拉取间隔(秒)，严格限频
  pull_timeout: 5.0
  supports_ttl: true          # 供应商是否返回 TTL（91HTTP 支持）

pool:
  max_size: 500               # 循环队列容量

test_timeout: 3.0             # 代理连接超时
test_concurrency: 10          # 批量测试并发数
test_buffer: 20               # 待测批次有界队列容量（队满丢最旧，drops 累计）
test_workers: 5               # 测试 worker 数（缺省自动 max(1, test_concurrency//pull_count)）

ttl_sweep_interval: 5.0       # TTL 清扫周期
```

> 运行期真实配置 `config/level1_pool.yaml` 已 gitignore，由模板 `config/level1_pool.example.yaml` 复制并替换 `api_key`/`trade_no`。

## 11. 鲁棒性与容错

- **限频保证**：`pull_lock` 只保护拉取（供应商限频点），固定按 tick 起点计时 `sleep(pull_interval)`，测试耗时不再拖慢拉取节奏。
- **异常隔离**：供应商故障/网络抖动只影响当前 tick，循环不中断；连续失败记 WARN 日志；单个测试批次异常仅记日志并累计 `test_failures`。
- **内存有界**：待测批次用有界队列（`test_buffer`），队满丢最旧批次（`drops` 累计），环形池固定上限，无内存膨胀风险。
- **超时控制**：拉取、测试均设 aiohttp 超时，避免悬挂。
- **去重刷新**：按 `proxy_url` 去重，重复入池删除重建（刷新 ttl/region、新 id），`duplicates` 累计并随 `/status`（`total_duplicates`）暴露。
- **优雅启停**：`lifespan` 统一启停任务；收到退出信号取消任务、关闭连接。
- **空池/边界**：池空时 `all/after` 返回空数组；`after` 的 id 越界返回空数组。`/ips/after/{id}` 响应顶层携带 `max_id`（当前池内最大 id，池空为 null）：二级池在增量空响应时，仅当水位线大于 `max_id`（一级池重启/换代）才触发全量重拉，等于 `max_id` 视为暂无新 IP 不重拉。

## 12. 可扩展性

- 供应商：继承 `BaseProvider` + 工厂注册，新增供应商零侵入主流程。
- 测试原语：`tester` 依赖注入测试函数，可替换为其它验证策略。
- 淘汰策略：容量淘汰与 TTL 淘汰解耦，未来可加「质量评分淘汰」等。
- 参数：全部阈值/周期配置化，无需改码。

## 13. 测试策略

- `test_pool.py`：环形淘汰(满500弹最旧)、TTL 清扫、协议计数、`after(id)` 边界、`max_id`、`proxy_url` 去重（删除重建刷新、新 id、不同协议不同键）。
- `test_provider.py`：工厂按 type 选择、子类解析供应商响应为 `ProviderIp`（含 http91 的 `expire_time→ttl`、`protocol=2→SOCKS5`）、未知 type 报错。
- `test_tasks.py`：1s 拉取节奏、拉取异常不中断、worker 管线（并行/并发上限/队满丢批/慢测试不阻塞拉取）、TTL 清扫。
- `test_routes.py`：用 aioresponses 模拟供应商，验证 status/count 统计字段正确（含 `total_duplicates`）、api_call_count 递增。
- `mock_provider.py`：本地起一个模拟供应商服务，供集成联调与压测。

## 14. 实施步骤

1. 搭骨架：requirements、config、main 装配、公共库接入。
2. 实现 `Level1Pool` + 单测。
3. 实现 `BaseProvider/DefaultHttpProvider/ProviderFactory` + 单测。
4. 实现 `Tester/PullTask/TtlSweeper` + 拉取循环。
5. 实现 routes + 中间件统计 + 单测。
6. `mock_provider.py` 端到端联调，验证限频、环形淘汰、TTL。
